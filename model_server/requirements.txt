# Install ON the DataLab / hosting box (NOT in the main project .venv).
fastapi>=0.115
uvicorn>=0.30
transformers>=4.44
torch>=2.3
accelerate>=0.33
sentencepiece>=0.2
huggingface_hub>=0.24    # snapshot_download into agent_models/
bitsandbytes>=0.43       # only if LOAD_4BIT=1 (CUDA/Linux GPU required)
requests>=2.32           # used by test_server.py
